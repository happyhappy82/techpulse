---
interface Price {
  vendor: string;
  price: number;
  url: string;
  shipping?: string;
  badge?: string;
}

interface Props {
  prices: Price[];
  productName: string;
}

const { prices, productName } = Astro.props;

// 최저가 찾기
const lowestPrice = Math.min(...prices.map((p) => p.price));

// 가격을 포맷팅하는 함수
function formatPrice(price: number): string {
  return new Intl.NumberFormat('ko-KR').format(price);
}

// 최저가인지 확인
function isLowestPrice(price: number): boolean {
  return price === lowestPrice;
}
---

<div class="price-comparison">
  <div class="comparison-header">
    <h3 class="text-subtitle font-bold text-brand-text mb-2">
      💰 가격 비교
    </h3>
    <p class="text-caption text-brand-text-secondary">
      {productName}의 최저가를 찾아보세요
    </p>
  </div>

  <div class="price-list">
    {prices.map((priceInfo) => (
      <div class={`price-item ${isLowestPrice(priceInfo.price) ? 'lowest-price' : ''}`}>
        <div class="price-item-header">
          <div class="vendor-info">
            <span class="vendor-name">{priceInfo.vendor}</span>
            {priceInfo.badge && (
              <span class="vendor-badge">{priceInfo.badge}</span>
            )}
          </div>
          {isLowestPrice(priceInfo.price) && (
            <span class="lowest-badge">최저가 🔥</span>
          )}
        </div>

        <div class="price-info">
          <div class="price-main">
            <span class="price-amount">{formatPrice(priceInfo.price)}원</span>
            {priceInfo.shipping && (
              <span class="shipping-info">{priceInfo.shipping}</span>
            )}
          </div>

          <a
            href={priceInfo.url}
            target="_blank"
            rel="nofollow noopener noreferrer"
            class="price-button"
          >
            구매하기
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </a>
        </div>
      </div>
    ))}
  </div>

  <div class="comparison-note">
    <p class="text-small text-brand-text-secondary">
      ℹ️ 가격은 실시간으로 변동될 수 있습니다. 최종 가격은 판매처에서 확인해주세요.
    </p>
  </div>
</div>

<style>
  .price-comparison {
    background: linear-gradient(to bottom right, #F8FAFC, #FFFFFF);
    border: 1px solid var(--brand-border);
    border-radius: 12px;
    padding: 24px;
    margin: 24px 0;
  }

  .comparison-header {
    margin-bottom: 20px;
    padding-bottom: 16px;
    border-bottom: 2px solid var(--brand-border);
  }

  .price-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 16px;
  }

  .price-item {
    background: white;
    border: 2px solid var(--brand-border);
    border-radius: 8px;
    padding: 16px;
    transition: all 0.3s ease;
  }

  .price-item:hover {
    border-color: var(--brand-primary);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.1);
    transform: translateY(-2px);
  }

  .price-item.lowest-price {
    background: linear-gradient(135deg, #FEF3C7 0%, #FEE2E2 100%);
    border-color: var(--brand-accent);
    border-width: 3px;
  }

  .price-item-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
  }

  .vendor-info {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .vendor-name {
    font-size: 16px;
    font-weight: 700;
    color: var(--brand-text);
  }

  .vendor-badge {
    display: inline-block;
    padding: 2px 8px;
    background: var(--brand-primary);
    color: white;
    font-size: 11px;
    font-weight: 600;
    border-radius: 4px;
  }

  .lowest-badge {
    display: inline-block;
    padding: 4px 12px;
    background: linear-gradient(135deg, var(--brand-accent) 0%, #059669 100%);
    color: white;
    font-size: 12px;
    font-weight: 700;
    border-radius: 20px;
    box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.9;
      transform: scale(1.05);
    }
  }

  .price-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
  }

  .price-main {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .price-amount {
    font-size: 24px;
    font-weight: 800;
    color: var(--brand-text);
    letter-spacing: -0.5px;
  }

  .shipping-info {
    font-size: 13px;
    color: var(--brand-text-secondary);
    font-weight: 500;
  }

  .price-button {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 10px 20px;
    background: linear-gradient(135deg, var(--brand-primary) 0%, var(--brand-secondary) 100%);
    color: white;
    font-size: 14px;
    font-weight: 700;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .price-button:hover {
    transform: translateX(4px);
    box-shadow: 0 4px 16px rgba(37, 99, 235, 0.4);
  }

  .comparison-note {
    padding-top: 16px;
    border-top: 1px solid var(--brand-border);
  }

  /* 반응형 */
  @media (max-width: 768px) {
    .price-comparison {
      padding: 16px;
    }

    .price-info {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
    }

    .price-button {
      width: 100%;
      justify-content: center;
    }

    .price-amount {
      font-size: 20px;
    }
  }
</style>
